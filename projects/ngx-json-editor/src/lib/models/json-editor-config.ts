export interface JsonEditorConfig {
	buttons?: {
		format?: boolean;
		minify?: boolean;
		sort?: boolean;
		search?: boolean;
		copy?: boolean;
		download?: boolean;
		reset?: boolean;
		load?: boolean;
	};
}
